**맞아! 환경 설정부터!** 🔧

---

## 🛠️ Step-by-Step 환경 구성

### 1️⃣ Next.js 프로젝트 생성

```bash
cd /home/claude/day30-team-wiki

# Next.js 프로젝트 생성
npx create-next-app@latest . --typescript --tailwind --app --no-src-dir --import-alias "@/*"
```

---

### 2️⃣ shadcn/ui 설치

```bash
# shadcn/ui 초기화
npx shadcn@latest init

# 질문:
# Style: Default
# Color: Slate
# CSS variables: Yes
```

---

### 3️⃣ shadcn 컴포넌트 설치

```bash
# 한 줄로!
npx shadcn@latest add button card badge input textarea select dropdown-menu dialog alert separator scroll-area tabs avatar tooltip
```

---

### 4️⃣ NextAuth.js 설치

```bash
npm install next-auth @auth/core
npm install @auth/drizzle-adapter
```

---

### 5️⃣ 데이터베이스 (Drizzle + PostgreSQL)

```bash
# Drizzle ORM
npm install drizzle-orm
npm install -D drizzle-kit

# PostgreSQL driver
npm install postgres

# 또는 더 간단하게: better-sqlite3 (로컬 개발용)
npm install better-sqlite3
npm install -D @types/better-sqlite3
```

---

### 6️⃣ 에디터 (Tiptap)

```bash
npm install @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-image @tiptap/extension-placeholder
```

---

### 7️⃣ 추가 패키지

```bash
# 아이콘
npm install lucide-react

# 유틸리티
npm install clsx tailwind-merge

# 날짜
npm install date-fns

# OpenRouter AI
# (API 호출만 하면 되니 별도 패키지 불필요)
```

---

### 8️⃣ 환경 변수 설정

```bash
# .env.local 생성
cat > .env.local << 'EOF'
# Database
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl

# OpenRouter
OPENROUTER_API_KEY=your-openrouter-key
EOF
```

**NEXTAUTH_SECRET 생성:**
```bash
# 터미널에서 실행
openssl rand -base64 32
# 나온 값을 NEXTAUTH_SECRET에 복사
```

---

### 9️⃣ .gitignore 확인

```bash
# 확인
cat .gitignore | grep -E "\.env|\.db"

# 없으면 추가
echo ".env.local" >> .gitignore
echo "*.db" >> .gitignore
echo "*.db-*" >> .gitignore
```

---

### 🔟 폴더 구조 생성

```bash
# 폴더 생성
mkdir -p components/editor
mkdir -p components/document
mkdir -p components/sidebar
mkdir -p components/auth
mkdir -p lib/db
mkdir -p lib/auth
mkdir -p lib/api
mkdir -p types
mkdir -p app/api/auth/[...nextauth]
mkdir -p drizzle
```

---

### 1️⃣1️⃣ Drizzle 설정

```bash
# drizzle.config.ts 생성
cat > drizzle.config.ts << 'EOF'
import type { Config } from 'drizzle-kit';

export default {
  schema: './lib/db/schema.ts',
  out: './drizzle',
  driver: 'better-sqlite3',
  dbCredentials: {
    url: './dev.db',
  },
} satisfies Config;
EOF
```

---

### 1️⃣2️⃣ 테스트 실행

```bash
npm run dev

# 브라우저에서 확인
# http://localhost:3000

# Ctrl+C로 종료
```

---

## ✅ 환경 구성 체크리스트

```
□ Next.js 프로젝트 생성
□ TypeScript 설정
□ Tailwind CSS 설정
□ shadcn/ui 설치
□ 컴포넌트 12개 설치
□ NextAuth.js 설치
□ Drizzle ORM 설치
□ Tiptap 에디터 설치
□ 추가 패키지 설치
□ .env.local 생성
□ 폴더 구조 생성
□ npm run dev 테스트
```

---

## 🎯 다음 단계

환경 설정 완료 후:

1. **Clarify #1 인터뷰**
2. **병렬 문서 생성**
3. **CLAUDE.md**
4. **IMPLEMENTATION.md**
5. **구현 시작!**