# 수동으로 로컬디비 생성하고
docker exec -it budget-tracker-db psql -U budget -d postgres -c "CREATE DATABASE helpdesk;"

docker exec -it budget-tracker-db psql -U budget -d helpdesk

DATABASE_URL="postgresql://budget:budget123@193.168.195.222:5432/helpdesk"
DATABASE_DIRECT_URL="postgresql://budget:budget123@193.168.195.222:5432/helpdesk"

# 돌려봤더니 drizzle 오류남. 현상 보여주고 알아서 고치라고 함
npx drizzle-kit push
# .env 설정파일부터 이것저것 오류발생. 알아서 고치고 빌드다시시킴
# 수작업으로 했는데....이것도 agent에 맞겨야할지..